angular.module('studying-node')

    .factory('AlertService', function(){

       /* $scope.alerts = {};

        var addAlert = function (type, content) {
            $scope.alerts.push({type:type, msg: content});
        };

        var doAlert = function (content, level) {
            if (!!content && content.constructor === Array && content.length > 0) {
                angular.forEach(content, function (item) {
                    addAlert(item, level);
                });
            } else if (!!content && content.constructor !== Array && !!content) {
                addAlert(content, level);
            }
        };

        var success = function (content) {
            doAlert(content, 'success');
        };

        var warning = function (content) {
            doAlert(content, 'warning');
        };

        var error = function (content) {
            doAlert(content, 'alert');
        };
        
        return {
            success: success,
            warning: warning,
            error: error
        }*/
        return null;

    });